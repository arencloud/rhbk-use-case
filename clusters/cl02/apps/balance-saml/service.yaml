apiVersion: v1
kind: Service
metadata:
  name: balance-saml
  namespace: balance-saml
  annotations:
    argocd.argoproj.io/sync-wave: "0"
  labels:
    app.kubernetes.io/name: balance-saml
    app.kubernetes.io/part-of: arencloud-identity
spec:
  selector:
    app.kubernetes.io/name: balance-saml
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
